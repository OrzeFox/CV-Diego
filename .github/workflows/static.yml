# Static site configuration for GitHub Pages
# This ensures proper serving of HTML, CSS, and JavaScript files

# Enable static file serving
static_files:
  - index.html
  - index-en.html
  - styles.css
  - scripts/
  - .nojekyll

# Disable Jekyll processing
jekyll: false
